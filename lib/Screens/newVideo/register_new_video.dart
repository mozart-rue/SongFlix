import 'package:flutter/material.dart';
import 'package:song_flix/Screens/newVideo/widgets/body.dart';

class RegisterNewVideo extends StatelessWidget {
  const RegisterNewVideo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Body(),
    );
  }
}
